<%- include html.ejs %>
<body>
    <%- include header.ejs %>

    <div class="row">
        <div class="col-lg-12">
            <h1>par étages</h1>
        </div>
    </div>

    <% for ( var floor in def.floors ) {
        var rooms =def.floors [floor] ;
    %>
    <div class="row">
        <div class="col-md-8">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title"><i class="fa fa-bar-chart-o"></i> <%= floor %></h3>
                </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-lg-4">
                            <div class="panel panel-default">
                                <div class="panel-body">&nbsp;</div>
                            </div>
                        </div>
                        <button id="<%= floor %>_1" type="button" class="btn btn-primary btn-lg btn-success">Ouvrir</button>
                        <button id="<%= floor %>_2" type="button" class="btn btn-primary btn-lg btn-warning">a demi</button>
                        <button id="<%= floor %>_0" type="button" class="btn btn-primary btn-lg btn-danger">Fermer</button>
                    </div>
                    <script>
                        <% for ( var ip =0 ; ip <= 2 ; ip++ ) { %>
                        $('#<%= floor %>_<%= ip %>').click (function (evt) {
                            evt.stopPropagation () ;
                            $.ajax ({
                                url: 'http://' + window.location.host + '/floor/<%= floor %>/<%= ip %>',
                                type: 'get',
                                //data: JSON.stringify ({}),
                                //contentType: 'application/json',
                                complete: null
                            }).done (function (data) {
                            }) ;
                        }) ;
                        <% } %>
                    </script>

                </div>
            </div>
        </div>
        <div class="col-md-4">

        </div>
    </div>
    <% } %>

    <%- include footer.ejs %>
</body>
</html>